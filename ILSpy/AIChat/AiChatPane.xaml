<UserControl x:Class="ICSharpCode.ILSpy.AIChat.AiChatPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ICSharpCode.ILSpy.ViewModels"
             xmlns:chat="clr-namespace:ICSharpCode.ILSpy.AIChat"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="360"
             d:DataContext="{d:DesignInstance chat:AiChatPaneModel}"
             viewModels:Pane.IsActive="{Binding IsActive}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<ListBox x:Name="MessagesList"
				 Grid.Row="0"
				 ItemsSource="{Binding Messages}"
				 HorizontalContentAlignment="Stretch"
				 VirtualizingStackPanel.IsVirtualizing="False"
				 ScrollViewer.CanContentScroll="False"
				 ScrollViewer.VerticalScrollBarVisibility="Auto"
				 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
				 BorderThickness="0"
				 Margin="0,0,0,6"
				 Loaded="MessagesList_Loaded">
			<ListBox.ItemTemplate>
				<DataTemplate DataType="{x:Type chat:AiChatMessage}">
					<Border Margin="2" Padding="6" HorizontalAlignment="Stretch" BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" BorderThickness="1">
						<TextBox Text="{Binding DisplayText, Mode=OneWay}"
								 IsReadOnly="True"
								 BorderThickness="0"
								 Background="Transparent"
								 IsTabStop="False"
								 HorizontalAlignment="Stretch"
								 MinWidth="0"
								 TextWrapping="Wrap"
								 AcceptsReturn="True"
								 VerticalScrollBarVisibility="Disabled"
								 HorizontalScrollBarVisibility="Disabled"
								 SizeChanged="MessageTextBox_SizeChanged"
								 PreviewMouseWheel="MessageTextBox_PreviewMouseWheel" />
					</Border>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

		<TextBox Grid.Row="1"
				 Margin="0,0,0,6"
				 MinHeight="70"
				 AcceptsReturn="True"
				 TextWrapping="Wrap"
				 VerticalScrollBarVisibility="Auto"
				 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
				 PreviewKeyDown="InputBox_PreviewKeyDown" />

		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
			<CheckBox Content="Enter to send" IsChecked="{Binding SendOnEnter, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0" />
			<Button Content="Send" Command="{Binding SubmitCommand}" Margin="0,0,6,0" MinWidth="72" />
			<Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,6,0" MinWidth="72" />
			<Button Content="Clear" Command="{Binding ClearCommand}" MinWidth="72" />
		</StackPanel>
	</Grid>
</UserControl>
