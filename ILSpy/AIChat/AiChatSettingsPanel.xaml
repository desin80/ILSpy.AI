<UserControl x:Class="ICSharpCode.ILSpy.AIChat.AiChatSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:chat="clr-namespace:ICSharpCode.ILSpy.AIChat"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="640"
             d:DataContext="{d:DesignInstance chat:AiChatSettingsViewModel}">
	<StackPanel Orientation="Vertical">
		<GroupBox Header="AI Chat">
			<StackPanel Margin="10">
				<StackPanel Orientation="Horizontal" Margin="0,0,0,8">
					<TextBlock VerticalAlignment="Center" Width="80" Text="Provider:" />
					<ComboBox Width="260"
							  ItemsSource="{Binding ProviderItems}"
							  SelectedValuePath="Kind"
							  DisplayMemberPath="Name"
							  SelectedValue="{Binding Settings.Provider, Mode=TwoWay}" />
				</StackPanel>

				<TextBlock FontWeight="SemiBold" Text="OpenAI-compatible" Margin="0,0,0,4" />
				<StackPanel Orientation="Horizontal" Margin="0,0,0,4">
					<TextBlock VerticalAlignment="Center" Width="80" Text="Base URL:" />
					<TextBox Width="520" Text="{Binding Settings.OpenAIBaseUrl, UpdateSourceTrigger=PropertyChanged}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="0,0,0,4">
					<TextBlock VerticalAlignment="Center" Width="80" Text="Model:" />
					<TextBox Width="520" Text="{Binding Settings.OpenAIModel, UpdateSourceTrigger=PropertyChanged}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="0,0,0,8">
					<TextBlock VerticalAlignment="Center" Width="80" Text="API Key:" />
					<PasswordBox x:Name="ApiKeyPasswordBox" Width="520" PasswordChanged="ApiKeyPasswordBox_PasswordChanged" />
				</StackPanel>

				<TextBlock FontWeight="SemiBold" Text="Codex CLI" Margin="0,8,0,4" />
				<StackPanel Orientation="Horizontal" Margin="0,0,0,4">
					<TextBlock VerticalAlignment="Center" Width="80" Text="Path:" />
					<TextBox Width="520" Text="{Binding Settings.CodexCliPath, UpdateSourceTrigger=PropertyChanged}" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<TextBlock VerticalAlignment="Center" Width="80" Text="Arguments:" />
					<TextBox Width="520" Text="{Binding Settings.CodexCliArguments, UpdateSourceTrigger=PropertyChanged}" />
				</StackPanel>
			</StackPanel>
		</GroupBox>
	</StackPanel>
</UserControl>
